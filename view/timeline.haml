!!!
%html
  %head
    %title
      Goodreads timeline for user ID
      = @user_id
    %link{:href => '/ext/style.css', :rel => 'stylesheet',
          :type => 'text/css'}
  %body
    %h1
      Goodreads timeline for user ID
      %a{:href => ""}= @user_id
    %p
      They have read
      = @all_reviews.search('review').length
      books over
      = (@by_day.keys.max - @by_day.keys.min)
      days from
      = @by_day.keys.min
      to
      = @by_day.keys.max.to_s + '.'
    - @by_month.keys.sort.each do |month|
      = partial(:_calendar, {:reviews => @by_month[month]})
